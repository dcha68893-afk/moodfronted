/*
  Content-Security-Policy: default-src 'self' https://www.gstatic.com https://www.googleapis.com https://firebase.googleapis.com https://firestore.googleapis.com https://.firebaseapp.com https://.googleapis.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com; img-src * data: blob:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; script-src 'self' 'unsafe-inline' https://www.gstatic.com https://www.googleapis.com https://firebase.googleapis.com https://firestore.googleapis.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://accounts.google.com; frame-src 'self' https://accounts.google.com https://nexorah-xnv6.onrender.com; connect-src 'self' https://nexorah-xnv6.onrender.com wss://nexorah-xnv6.onrender.com https://api.cloudinary.com https://www.googleapis.com https://accounts.google.com https://firebase.googleapis.com https://firestore.googleapis.com;
  # FIX (stale CSP domain): this previously allowlisted https://nexora-3bla.onrender.com,
  # which doesn't match the BACKEND_URL actually configured in .env
  # (https://nexorah-xnv6.onrender.com). If your host (Netlify/Cloudflare Pages/Render
  # Static Site) honors this _headers file, that mismatch would silently block every
  # fetch/websocket call to your real backend under CSP — including the Google
  # sign-in POST to /api/auth/google. Note: this file has NO effect if you're deployed
  # as a Render "Web Service" running server.js, since helmet's CSP is disabled there
  # (contentSecurityPolicy: false) — only one of the two is actually enforced depending
  # on your deployment type, so keep whichever one is live in sync with BACKEND_URL.