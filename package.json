{
  "name": "uniconnectsphere",
  "version": "1.0.5",
  "description": "Nexipa/Kynecta frontend — static PWA client (Vanilla JS)",
  "private": true,
  "scripts": {
    "build": "node scripts/build-config.js",
    "start": "npx http-server dist -p 5500"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
